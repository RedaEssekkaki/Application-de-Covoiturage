{% extends 'base.html.twig' %}

{% block title %}{% trans %}Supprimer le trajet{% endtrans %} "{{ trajet.villeDepart }} - {{ trajet.villeDestination }}"{% endblock %}
        {% block stylesheets %}
            {{ parent() }}
            <link rel="stylesheet" href="{{ asset('css/deleteTrajet.css') }}">
        {% endblock %}
{% block body %}
    <div class="container">
        <h1>{% trans %}le trajet{% endtrans %} "{{ trajet.villeDepart }} - {{ trajet.villeDestination }}" ?</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        <a href="{{ path('trajet.show', {'id': trajet.id}) }}" class="btn btn-primary">{% trans %}Annuler{% endtrans %}</a>
    </div>
{% endblock %}
